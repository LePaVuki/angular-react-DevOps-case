daemon off;
pid /tmp/nginx.pid;

events {

    worker_connections   2000;

}

http {
  include mime.types;

  server {
    listen 8080;

    location / {

      root /var/www/html/browser;
      index index.html;

    }

  }
}